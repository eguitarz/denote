{{note-list
  notes=notes
  openedNote=openedNote
  selectNote=(route-action "loadNote")
  createNote=(route-action "createNote")
}}
{{note-editor
  notes=notes
  openedNote=openedNote
  resizeComments=(route-action "resizeComments")
}}
{{outlet}}
