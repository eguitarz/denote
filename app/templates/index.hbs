{{text-editor
  contenteditable=true
  setEditor=(route-action "setEditor")
  save=(route-action "save")
  createSideNote=(route-action "createSideNote")
}}
{{side-note-page
  page=model
}}
{{outlet}}
